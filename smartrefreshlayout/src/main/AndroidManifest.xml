<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hy.junl.smartrefreshlayout">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/title_activity_index_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.BannerPracticeActivity"
            android:label="@string/title_activity_practice_banner"
            android:screenOrientation="portrait" />

        <activity
            android:name=".activity.BasicUsingActivity"
            android:label="@string/title_activity_using_basic"
            android:screenOrientation="portrait" />
    </application>

</manifest>